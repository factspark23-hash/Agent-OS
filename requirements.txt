# Agent-OS Requirements
# Core dependencies (install all of these)

# Browser automation
playwright==1.49.1

# WebSocket + HTTP server
websockets==13.1
aiohttp==3.11.11

# HTTP client (used by connectors)
httpx>=0.27.0

# Security & encryption
cryptography>=44.0.0

# HTML parsing (scanners)
beautifulsoup4>=4.12.3
lxml>=5.3.0

# Configuration
pyyaml>=6.0.2

# Utilities
requests>=2.32.3
psutil>=6.1.0

# Human mimicry
numpy>=1.26.4

# MCP protocol support (for Claude Desktop / Codex)
mcp>=1.0.0

# Testing
pytest>=8.3.4
pytest-asyncio>=0.25.0

# ─── Optional (install separately if needed) ─────────────────
# Video/audio transcription:
#   pip install yt-dlp openai-whisper
#   OR install whisper.cpp for faster local transcription
